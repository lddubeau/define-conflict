This is a repository created to hold files illustrating an issue report for RequireJS.

The problem is that if there is an element in the HTML with the ``id`` set to ``define``, RequireJS won't load.
